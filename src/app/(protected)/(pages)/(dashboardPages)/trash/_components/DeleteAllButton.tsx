'use client';
import { Project } from '@prisma/client';
import React from 'react';

type Props = {
  projects: Project[];
};

const DeleteAllButton = (props: Props) => {
  return <div>DeleteAllButton</div>;
};

export default DeleteAllButton;
